{
  "name": "mudit-traders",
  "private": true,
  "version": "0.1.0",
  "description": "Store management system for Mudit Traders, Akola, Chittorgarh, Rajasthan",
  "scripts": {
    "postinstall": "npm run install:all",
    "build": "npm run build:client",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "test:server": "npm test --prefix server",
    "seed": "npm run seed --prefix server",
    "build:server": "npm run build --prefix server",
    "build:client": "npm run build --prefix client && rm -rf dist && cp -r client/dist dist"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.5.1",
    "morgan": "^1.10.0",
    "zod": "^3.23.8"
  }
}
